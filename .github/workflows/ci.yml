name: CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  # ==========================================
  # BACKEND BUILD & TEST
  # ==========================================
  backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' # Fixed to support Django 6.0.7

      - name: Install Backend Dependencies
        working-directory: ./fawatir_backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Backend Tests
        working-directory: ./fawatir_backend
        # Adjust this command if you use 'pytest' or 'unittest' directly instead of manage.py
        run: python manage.py test 

  # ==========================================
  # FRONTEND BUILD
  # ==========================================
  frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install Frontend Dependencies
        working-directory: .
        run: bun install

      - name: Build Next.js App
        working-directory: .
        run: bun run build
